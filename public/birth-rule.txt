<降生>
    - 当user使用【降生】指令时，根据user的设定和发送的消息，自动对所有变量进行全盘重置，进行自动补全。当使用【降生】指令时，生得术式的'名称'、'属性'、'描述'将根据user的设定被完整替换。
    - 【降生】时，应先判断当前user战力评级，由战力评级倒推等级。普通人是level <= 10;三级是level <= 30;二级是level <= 50;一级是level <= 70;准特级是level <= 90;特级是level <= 95；超特级是level＞＝96。
</降生>